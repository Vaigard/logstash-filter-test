input {
	udp {
		port => 8082
	}
}

input {
	file {
		path => "/usr/share/logstash/input.json"
		codec => json
		start_position => "beginning"
		sincedb_path => "/usr/share/logstash/since_db"
	}
}


output {
	stdout {}
	file {
		codec => "json"
		path => "/usr/share/logstash/output.json"
	}	
}
